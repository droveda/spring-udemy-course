# logging.level.org.springframework=DEBUG
management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
  data:
    jpa:
      repositories:
        bootstrap-mode: default

#logging:
#  level:
#    root: debug